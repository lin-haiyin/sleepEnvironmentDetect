#pragma once

// Copy to secrets.h (ignored by git) for a future Wi-Fi/MQTT provisioning
// module. Never commit real credentials.
#define ENV_WIFI_SSID "replace-me"
#define ENV_WIFI_PASSWORD "replace-me"
#define ENV_MQTT_URI "mqtt://192.168.1.10:1883"
#define ENV_MQTT_USERNAME ""
#define ENV_MQTT_PASSWORD ""
#define ENV_MQTT_DEVICE_ID "env-s3-01"
// Keep empty for mqtt://1883. For mqtts://8883, define a PEM CA string here
// or include it from a separate ignored header.
#define ENV_MQTT_ROOT_CA_PEM nullptr
